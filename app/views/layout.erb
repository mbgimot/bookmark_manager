<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Bookmark Manager</title>
  </head>

  <body>
    <% if current_user %>
      Welcome, <%= current_user.email %>
    <% end %>
    <% if flash[:error] %>
      <%=flash[:error]%>
    <%end%>
    <%= yield %>
  </body>
</html>
